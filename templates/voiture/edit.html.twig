{% extends 'layout.html.twig' %}

{% block body %}
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    <div class="row mt-4">
        <div class="col">
            {{ form_row(form.marque) }}
        </div>
        <div class="col">
            {{ form_row(form.color) }}
        </div>
    </div>
    {{ form_rest(form) }}
{% endblock %}

{% block title %}

{% endblock %}

